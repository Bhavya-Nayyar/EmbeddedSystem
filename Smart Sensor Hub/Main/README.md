The main folder with all the C and ESP-IDF code. It contains the code for every device as a separate module in the form of header files in the custom_components folder, which are then included in the main.c file.
